<sql driver="com.mysql.jdbc.Driver" url="jdbc:mysql://${db.host}/${db.name}" userid="${db.login}"
			password="${db.password}">
  <classpath>
    <fileset dir="../../lib">
      <include name="**/*.jar"/>
    </fileset>
  </classpath>
  
  <transaction>
    DELETE FROM ox_data_raw_tracker_impression
  </transaction>
  <transaction>
    DELETE FROM ox_data_raw_tracker_variable_value
  </transaction>
</sql>