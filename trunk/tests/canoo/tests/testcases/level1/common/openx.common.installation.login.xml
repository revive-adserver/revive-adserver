				<ifStep description="update tests - if the application is updated then the administrator login screen is displayed during the installation">
				  <condition>
					<verifyText description="Check text on page"
						text="Administrator Login" />
				  </condition>
				  <then>
				  	&universal;
					<setInputField description="Set login field"
						name="username" value="${admin.login}" />
					<setInputField description="Set password field"
						name="password" value="${admin.pass}" />
					<clickButton description="Click 'Continue »' button"
						name="btn_dbsetup"
						save="login_clickCont" />
				  </then>
				  <else>
					<!-- do something else instead -->
				  </else>
				</ifStep>
