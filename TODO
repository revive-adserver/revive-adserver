phpAds(New) - Todo

Feature which should be implemented 
in the next release (2.0):
  + Zones
  + Report plugins
  + Availability
  + Banner invocation script builder
  + XML-RPC invocation

Feature which should be implemented
in the nearby future (2.1/2.2)
  + Move to PHP4 only? Mybe checking PHP version for 
    concerning expansion or reduction of functions
  + Use an template system for admin interface
  + Support for right-left scripts such as arabic
  + Cookie based sequential banner retrieval
    (won't work with cookies! better SessionID or so)
  + installation and update scripting

Long term features:
  - Unknown
  
